import 'package:flutter/material.dart';

class Second_wake extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Row(
        children: [
          Text('Second_wake')
        ],
      ),
    );
  }
}