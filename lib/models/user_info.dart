class User_info {
  String? email;    // 이메일 주소
  String? pw;       // 비밀번호
  String? name;     // 이름
  String? weight;   // 몸무게
  String? adr;     // 주소

  User_info(this.email, this.pw, this.name);
}